targets:
    - "//ios-app:ios-app"
    - "//ios-app:UnitTests"

targetConfig:
    "//ios-app:UnitTests":
        environmentVariables: 
            var: "--some"
        commandLineArguments: 
            - "--Some"
projects:
    "SnapshotMe":
        paths:
            - "ios-app/**"

    "Deps":
        paths:
            - "Vendor/**"
